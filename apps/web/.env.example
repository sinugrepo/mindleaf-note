# Mindleaf Web (frontend) environment variables

# VITE_API_URL: The backend Hono server URL.
# In dev, Vite proxies /api to this URL so HttpOnly cookies work.
# In prod, Caddy reverse-proxies /api so this is rarely needed.
VITE_API_URL="http://localhost:8787"
